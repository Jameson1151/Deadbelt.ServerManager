<Window x:Class="Deadbelt.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding ApplicationName}"
        Height="650"
        Width="1100"
        MinHeight="650"
        MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Brush.Background}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="0,0,0,1">

            <StackPanel Margin="25,15">
                <TextBlock Text="{Binding ApplicationName}"
                           FontSize="32"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource Brush.TextPrimary}"/>

                <TextBlock Text="{Binding ApplicationSubtitle}"
                           FontSize="14"
                           Foreground="{StaticResource Brush.TextSecondary}"/>
            </StackPanel>

        </Border>

        <Grid Grid.Row="1">
            <StackPanel Width="350"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <TextBlock Text="{Binding WorkspaceStatus}"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource Brush.TextPrimary}"
                           Margin="0,0,0,20"
                           TextAlignment="Center"/>

                <TextBlock Text="{Binding WelcomeMessage}"
                           FontSize="15"
                           Foreground="{StaticResource Brush.TextSecondary}"
                           TextAlignment="Center"
                           Margin="0,0,0,40"/>

                <Button Content="Create Workspace"
                        Command="{Binding CreateWorkspaceCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Margin="0,0,0,15"/>

                <Button Content="Open Workspace"
                        Command="{Binding OpenWorkspaceCommand}"
                        Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="1,1,1,0">

            <TextBlock Text="{Binding StatusMessage}"
                       Margin="15,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource Brush.TextSecondary}"/>
        </Border>

    </Grid>
</Window>